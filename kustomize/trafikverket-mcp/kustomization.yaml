apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mcp
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
# Create the secret out-of-band from secrets-template.yaml – never commit real values.
