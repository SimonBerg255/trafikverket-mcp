# Template only – fill in and apply with kubectl, do NOT commit the filled file.
apiVersion: v1
kind: Secret
metadata:
  name: trafikverket-mcp-secrets
  namespace: mcp
type: Opaque
stringData:
  TRAFIKVERKET_API_KEY: "<free key from https://data.trafikverket.se/>"
  MCP_SERVER_JWT_SECRET: "<python3 -c 'import secrets; print(secrets.token_hex(32))'>"
